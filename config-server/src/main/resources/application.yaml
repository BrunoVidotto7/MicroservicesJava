server:
  port: 8888
spring:
  application:
    name: vidotto-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BrunoVidotto7/vidotto-config-server
          default-label: main
          search-paths:
            - 'greeting-service*'